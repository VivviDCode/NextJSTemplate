/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "cdn.dummyjson.com",
      "robohash.org",
      "avatars.githubusercontent.com",
    ],
  },
};

module.exports = nextConfig;
